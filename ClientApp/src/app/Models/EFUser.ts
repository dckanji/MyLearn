/** 
 * typescript的寫法,如同建立一個 映射類
 * 可承接來自http端的資料結構
 * 不分大小寫...但不能含底線
*/
export class EFUser {
    user_id: number;
    useR_NAME: string;
    user_age: number;
    creation_date: Date;
    usertest:  string;
}